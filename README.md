# Express + MySQL CRUD App

This project is a simple Node.js application using **Express** and **MySQL** to perform CRUD operations.  
It follows the requirements of the Practical Quiz #1.

## ✔ Features
- Add a user (POST /add)
- Get all users (GET /records)
- Get one user by ID (GET /record/:id)
- Update user (PUT /update/:id)
- Delete user (DELETE /delete/:id)

